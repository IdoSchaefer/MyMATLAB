load toda
%tar = pi^(-1/4)*(exp(-(x - 5).^2/2) + exp(-(x + 1).^2/2));
%tar = fi0 + pi^(-1/4)*(exp(-(x - 7).^2/2))*sqrt(dx);
tar = (P(:, 3)+P(:,8))/sqrt(2);
%tar = tar/norm(tar);
[field, psi, relE, conv, niter, convprop, npropconv, mallniterc, J1, maxgrad, weight] = OCrlxcp(fi0, tar, Vf,...
    [-10 85], xdomain, @(t) 0.1, 0.1, 1, 100, 0.05, 5, 9, 1e-3);
[fieldK, psiK, relEK, convK, niterK, mniterK] = OCcheb(fi0, tar, Vf, [-10 85], xdomain, @(t) 0.1, 0.1, 100, 0.05, 5, 9, 1e-3);